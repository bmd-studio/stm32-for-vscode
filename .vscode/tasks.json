{
  "tasks": [
    {
      "label": "compile extension",
      "type": "shell",
      // "script": " echo",
      "command": "yarn webpack",
      "options": {
        // "cwd": "${workspaceRoot}",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "compile tests",
      "type": "shell",
      // "script": " echo",
      "command": "yarn pretest",
      "options": {
        // "cwd": "${workspaceRoot}",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$tsc"
    },
  ]
}