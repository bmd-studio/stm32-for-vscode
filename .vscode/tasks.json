{
  "tasks": [
    {
      "label": "compile extension",
      "type": "shell",
      // "script": " echo",
      "command": "npm run compile",
      "options": {
        // "cwd": "${workspaceRoot}",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "compile tests",
      "type": "shell",
      // "script": " echo",
      "command": "npm run pretest",
      "options": {
        // "cwd": "${workspaceRoot}",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$tsc"
    },
  ]
}