{
  "tasks": [
    {
      "label": "compile extension",
      "type": "shell",
      // "script": " echo",
      "command": "echo && yarn webpack",
      "options": {
        // "cwd": "${workspaceRoot}",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}