{
  "tasks": [
    {
      "label": "compile extension",
      "type": "shell",
      // "script": " echo",
      "command": "echo && yarn webpack",
      "options": {
        // "cwd": "${workspaceRoot}",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "compile tests",
      "type": "shell",
      "command": "yarn pretest",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}